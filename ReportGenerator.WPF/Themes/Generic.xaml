<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:ReportGeneratorWPF">


    <Style TargetType="{x:Type local:PlaceholderTextBox}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:PlaceholderTextBox}">
                    <Border Padding="3" BorderBrush="Black" BorderThickness="1">
                        <Grid>
                            <TextBlock x:Name="PART_Placeholder" Foreground="DarkGray" FontStyle="Italic" Text="{TemplateBinding PlaceholderText}"/>
                            <TextBox x:Name="PART_TextBox" Background="{x:Null}" Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay, Path=Text, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
